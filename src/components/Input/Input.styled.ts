import styled from "styled-components";

const StyledInput = styled.input`
	background-color: var(--input-color);
	border: none;
	border-radius: 5px;
	padding: 8px;
	margin: 2px 0;
`;

export { StyledInput };
